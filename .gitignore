CODEX_RULES.md
HANDOFF.md
CLAUDE.md
CLAUDE_history.md

# X自動投稿の実行時状態（コミット不要）
scripts/machines_prev.json
scripts/x_post_result.json

# Python キャッシュ
scripts/__pycache__/
__pycache__/

# Claude Code ローカル設定（公開リポジトリに含めない）
.claude/

# 設計ドキュメント作業用（Codexに読ませるためリポジトリ内に置くが公開しない）
_design/

# ★ビルドの出力（コミットしない・公開もしない）★
#  .preview-site/ = 裏取り前の内容を確かめる写し（noindex・robots全面Disallow）
#  _site/         = Pagesへ出す成果物（保護CIが空から組み立てる）
.preview-site/
_site/
_site.next/
.site-build-*/

# ★出典ページの生データはコミットしない★（2026-07-30）
#   このリポジトリは公開されている。他サイトのHTML全文を置くのは転載にあたる。
#   検算に要る生データは手元にだけ残し、証拠ファイル側には
#   検証に必要な1行分の引用（表の1行など）だけを記録する。
assets/data/claim-evidence/raw/

# CIの診断用ログ（コミットしない）
cc.log
as.log

# Codexへ渡す確認依頼（自動生成・コミットしない）
_design/spec_review/
.publish.lock
.publish.lock.stale.*
.publish.lock.evicttest*
.publish.lock.stale_test
.publish-in-progress.json
.recover.lock
.push-pending.json
